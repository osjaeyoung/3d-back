import { join } from 'path';

export const FILE_UPLOAD_DIR = join(process.cwd(), 'file', 'upload');
export const MESHROOM_OUTPUT_DIR = join(process.cwd(), 'file', 'meshroom');
export const CLI_DIR = join(process.cwd(), 'cli', 'meshroom.py');
